import type { NextConfig } from 'next'

// Server-rendered and deployed via OpenNext: no `output: 'export'`, and no dev
// API rewrites — this app owns its own route handlers.
const nextConfig: NextConfig = {}

export default nextConfig
