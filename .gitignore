# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# Dependencies
node_modules

# Build outputs
dist
*.tsbuildinfo

# Nx
.nx/cache
.nx/workspace-data

# Next.js
.next
out

# SST
.sst

vite.config.*.timestamp*

# Claude Code
.claude/settings.local.json

# MacOS
.DS_Store

# Env
.env
.env.*
!.env.example

*.difypkg
.nx/polygraph
.claude/worktrees

# Spec Kit
.specify/feature.json

# Lint-coverage fixtures — materialised at test time, never committed. They are deliberately
# full of rule violations, so keeping them on disk would fail the repo's own gates.
tooling/lint-coverage/fixtures/generated
