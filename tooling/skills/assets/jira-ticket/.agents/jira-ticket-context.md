# Jira Ticket Context

<!--
This file is read automatically by the jira-ticket skill before it writes a
ticket. It holds this project's standing instructions — anything that should
apply to every ticket raised here, but that is not true of every project.

Nothing is set yet. Replace the "No custom instructions yet." line below with
real instructions when this project needs any; the skill treats that line as
"nothing configured" and falls back to the shared templates in
`references/ticket-types.md`.

Only put things here that differ from the shared skill. Site, project key,
board and epic are NOT custom instructions — they live in
`.agents/skills.config`.

Delete this comment block once the file is filled in.
-->

## Custom instructions

_No custom instructions yet._

<!--
Examples of what belongs here. Delete this block and write the real thing.

### Standard acceptance criteria

The bullets every Story and Task carries at the end of its CoS. The skill's
default is:

- Unit tests with at least 80% coverage
- Feature changes are sufficiently documented

Add, remove or reword them for this project — e.g. a quality-gate criterion if
this project runs one, or an accessibility criterion for a front-end.

### Required fields and labels

Any field this project always sets that the skill does not — a component, a
team label, a fix version.

### Wording and terminology

Names this project uses for its own surfaces, and any phrasing to avoid.

### Anything the board does differently

Section labels, statuses or a workflow that diverges from
`references/workflow.md`.
-->
